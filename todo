✅ Migrate on a new project basis with an updated dockerfile, poetry and the rest.
- Re-implement the whole codebase in clean architecture layers. (Stopped at security/usercache)
Current task: 
FastAPI -> Auth Middleware -> SessionManager (интерфейс) 
                             -> RedisOAuthSessionManager (имплементация)
                             -> RedisConnectionManager -> Redis



- In process: Make all methods REST compatible
- Write tests for existing methods with pytest, gauge
